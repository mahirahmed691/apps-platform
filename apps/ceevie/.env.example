# Do NOT hand-fill this. It is DERIVED automatically.
#
# 1. Put account secrets in the repo-root .env once (see /.env.example).
# 2. Run:  node scripts/fill-env.js ceevie <supabase-project-ref>
#    (new-app.sh does this for you on new apps.)
#
# The generator writes .env.local with every value below populated.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
ANTHROPIC_API_KEY=
NEXT_PUBLIC_SITE_URL=https://ceevie.co.uk
